- name: Download and extract file
  hosts: all
  become: true
  
  tasks:
    - name: Relocate root
      shell: cd /

    - name: Download file
      shell: sudo curl -o /terraform/file.zip https://storage.cloud.google.com/jimmy-playbooks/TerraformScriptZip
    
    - name: Extract file
      shell: sudo unzip /terraform/file.zip -d /terraform/
    
