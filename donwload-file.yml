- name: Download and extract a file
  become: true
  shell: |
    cd /
    curl -o /terraform/file.zip https://storage.cloud.google.com/jimmy-playbooks/TerraformScriptZip
    unzip /terraform/file.zip -d /terraform/
  args:
    executable: /bin/bash
